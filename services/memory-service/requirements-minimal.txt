# Minimal requirements for Memory Service without heavy ML dependencies
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.10.6
python-dotenv==1.0.1

# Memory storage without ML (only vector storage)
qdrant-client==1.18.0

# For future integration with external embedding services
httpx==0.28.1
